const tabs=document.getElementById('tabs');const nameEl=document.getElementById('name');const descEl=document.getElementById('description');const avatar=document.getElementById('avatar');const counter=document.getElementById('counter');const copyBtn=document.getElementById('copyBtn');counter.textContent=`${PEOPLE.length} PROFILI`;
function slugify(t){return t.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}
function selectPerson(i,update=true){const p=PEOPLE[i];if(!p)return;document.querySelectorAll('.tab').forEach((b,n)=>b.classList.toggle('active',n===i));nameEl.textContent=p.name;descEl.innerHTML=p.description||'';avatar.textContent=p.name.charAt(0).toUpperCase();if(update){history.replaceState(null,'',`?osoba=${encodeURIComponent(slugify(p.name))}`);window.scrollTo({top:document.getElementById('profile').offsetTop-20,behavior:'smooth'})}}
PEOPLE.forEach((p,i)=>{const b=document.createElement('button');b.className='tab';b.textContent=p.name;b.addEventListener('click',()=>selectPerson(i));tabs.appendChild(b)});
const target=new URLSearchParams(location.search).get('osoba');const idx=PEOPLE.findIndex(p=>slugify(p.name)===target);if(idx>=0)selectPerson(idx,false);else if(PEOPLE.length)selectPerson(0,false);
copyBtn.addEventListener('click',async()=>{await navigator.clipboard.writeText(location.href);copyBtn.textContent='LINK SKOPIOWANY ✓';setTimeout(()=>copyBtn.textContent='KOPIUJ LINK DO TEGO PROFILU',1800)});